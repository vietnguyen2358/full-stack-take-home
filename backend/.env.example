# OpenRouter (required) — get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Daytona sandbox (required for live preview) — https://app.daytona.io
DAYTONA_API_KEY=
DAYTONA_API_URL=https://app.daytona.io/api
DAYTONA_TARGET=us

# Supabase (optional — clone history persistence)
SUPABASE_URL=
SUPABASE_KEY=

# MCP server (optional — defaults to http://localhost:8001)
MCP_SERVER_URL=http://localhost:8001
